<h1> hi </h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> : <%= task.completed ? "completed" : "not completed" %></li>
  <% end %>
</ul>

<h2><%= link_to "create new task", new_task_path %></h2>
